export const THEME_INIT = 'THEME_INIT';

// Action creator for initializing the theme
export const onThemeInit = () => {
  return {
    type: THEME_INIT,
  };
};

// Add other action creators as needed
